const db = require('../../config')
const moment = require('moment')

const changeNameTarget = async (req,res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = changeNameTarget