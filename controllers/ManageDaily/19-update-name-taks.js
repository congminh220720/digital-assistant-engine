const db = require('../../config')
const moment = require('moment')

const updateNameTask = async (req,res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = updateNameTask